use bevy::prelude::*;

use crate::game::level::Durability;


// pub enum AnimationEventType {
// 	CharacterHit(),
// 	WallHit(),
// 	WallBreak(),
// }
// pub struct AnimationEvent(AnimationEventType);

// pub fn handle_animation_event(
//   events: EventReader<AnimationEvent>,
// ) {
//   for event in events.iter() {
    
//   }
// }
